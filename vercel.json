{
    "version": 2,
    "installCommand": "npm install --include=optional",
    "builds": [
        {
            "src": "src/app.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "(.*)",
            "dest": "src/app.js"
        }
    ]
}